<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 位置1（建议）：放在UserMapper2所在的包下面，会被自动扫描到，但需要在pom.xml中配置maven构建时的资源路径 -->
<mapper namespace="com.zhengjian.sample.springboot.mybatismulti.mapper2.UserMapper2">
    <insert id="addUser" parameterType="com.zhengjian.sample.springboot.mybatismulti.pojo.User">
        insert into user (username,address) values (#{username},#{address});
    </insert>

    <delete id="deleteUserById">
        delete from user where id=#{id}
    </delete>

    <update id="updateUserById" parameterType="com.zhengjian.sample.springboot.mybatismulti.pojo.User">
        update user set username=#{username},address=#{address} where id=#{id}
    </update>

    <select id="getAllUser" resultType="com.zhengjian.sample.springboot.mybatismulti.pojo.User">
        select * from user;
    </select>
</mapper>